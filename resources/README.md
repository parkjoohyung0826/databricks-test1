# Bundle resources

Define Databricks jobs, pipelines, and other deployable resources in YAML files in this directory.

When resource files are added, reference them from the root `databricks.yml` using its `include` mapping.
